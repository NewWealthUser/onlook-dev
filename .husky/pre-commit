#!/usr/bin/env sh

if command -v bun >/dev/null 2>&1; then
  bun run format && git add .
else
  echo "husky: bun not found in PATH, skipping formatting step. Install Bun from https://bun.sh to re-enable auto-formatting." >&2
fi
